# Database
DATABASE_URL="postgresql://goyal:goyal_dev_password@localhost:5433/goyal_eoi?schema=public"
DIRECT_URL="postgresql://goyal:goyal_dev_password@localhost:5433/goyal_eoi?schema=public"
# Auth
AUTH_SECRET="generate-a-secure-random-string-at-least-32-chars"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# AWS S3 / MinIO
S3_ENDPOINT="http://localhost:9000"
S3_REGION="us-east-1"
S3_BUCKET="goyal-eoi-documents"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadmin"
S3_FORCE_PATH_STYLE="true"

# Email (Brevo / Bravo)
BREVO_API_KEY=""
EMAIL_FROM="noreply@goyalprojects.com"
EMAIL_FROM_NAME="Goyal & Co. | Hariyana Group"

# Redis
REDIS_URL="redis://localhost:6379"

# Integration providers (mock | nsdl | msg91 | salesforce)
KYC_PROVIDER="mock"
SMS_PROVIDER="mock"
CRM_PROVIDER="mock"

# Encryption key for PII (32+ chars; required in production)
PII_ENCRYPTION_KEY="generate-a-32-byte-key-for-pii-fields"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"


cd "c:\Users\avula\OneDrive\Desktop\EOI_CP\packages\db"

$env:DATABASE_URL="postgresql://neondb_owner:npg_0XxFc3fUoYpt@ep-twilight-darkness-ahysvgnn.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

node -e "
const bcrypt = require('bcryptjs');
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();
(async () => {
  const email = 'superadmin@goyal.com';
  const password = 'Admin@123@';
  const name = 'Super Admin';
  const passwordHash = await bcrypt.hash(password, 12);
  await prisma.user.upsert({
    where: { email },
    update: { passwordHash, role: 'ADMIN', status: 'ACTIVE' },
    create: {
      email, name, passwordHash, role: 'ADMIN', status: 'ACTIVE',
      adminProfile: { create: {} },
    },
  });
  console.log('Admin ready:', email);
  await prisma.\$disconnect();
})();
"