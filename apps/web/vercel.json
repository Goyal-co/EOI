{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd ../.. && npm ci --include=dev",
  "buildCommand": "cd ../.. && npm run db:generate && npm run db:migrate:deploy && npm run build --workspace=@goyal/web",
  "crons": [
    {
      "path": "/api/cron/email-retry",
      "schedule": "0 0 * * *"
    }
  ]
}
